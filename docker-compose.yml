services:
  app:
    build: .
    container_name: nestjs_app
    restart: always
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: "postgresql://neondb_owner:npg_ZzV2ki9OLcRg@ep-frosty-base-adpis9dt-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"
    command: ["npm", "run", "start:migrate:prod"]

volumes:
    pgdata: